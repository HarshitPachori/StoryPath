package com.example.journal_app.service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
// @EnableCaching
public class CaffeineCacheTest {
    // @Autowired
    // private UserServiceImpl userService;

    // @Autowired
    // private CacheManager cacheManager;

    @Test
    public void whenCacheableMethodCalled_thenValueCached() {
        // String email = "harshit7pachori@gmail.com";
        //
        // // Call the method and cache the result
        // userService.getUserByEmail(email);
        //
        // // Check if the value is in the cache
        // Object cachedValue = cacheManager.getCache("users").get(email, User.class);
        // if (cachedValue != null) System.out.println(cachedValue.toString());
        // assertThat(cachedValue).isNotNull();
    }
}
